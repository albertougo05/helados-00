/**
 * ../caja/index.js
 */

import MensajeAlerta from '../utils/MensajeAlerta';


/**
 * Inicializa
 * 
 */
var initPage = function () {

    if (hayStatus) {
        MensajeAlerta(mensajeStatus , "success");
    }

}();
